# Vercel Deployment Environment Variables
# Copy this file to .env.vercel and update with your actual values
# Then configure these variables directly in the Vercel dashboard for security

NODE_ENV=production
PORT=5000
MONGO_URI=your_mongodb_connection_string_here
JWT_SECRET=your_strong_jwt_secret_here (at least 32 characters)
CLIENT_URL=https://your-frontend-url.vercel.app